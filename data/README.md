# Instancias de teste

Coloque aqui os arquivos de entrada dos grafos.

Formato esperado:

```text
u v
u v
u v
```

Cada linha valida deve conter dois inteiros representando uma aresta. A leitura
ignora linhas invalidas, remove self-loops, remove duplicatas e normaliza os IDs
originais para `0..n-1`.
